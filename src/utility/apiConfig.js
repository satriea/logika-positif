const BASE_URL = 'http://localhost:5014/api';

export const API_ENDPOINTS = {
  LOGIN: `${BASE_URL}/login`,
  BOOKS: `${BASE_URL}/books`
  // Kamu bisa tambah endpoint lain di sini nanti
};

export default BASE_URL;
